class Solution:
    def longestCommonPrefix(self, strs):
        strs.sort()
        first, last = strs[0], strs[-1]
        ans = ""
        for i in range(min(len(first), len(last))):
            if first[i] != last[i]:
                break
            ans += first[i]
        return ans
